{
  "name": "cross-window",
  "version": "1.0.0",
  "description": "Handling communication between windows/iframes.",
  "main": "index.js",
  "scripts": {
    "start": "hs -c-1 ./src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "UNLICENSED",
  "devDependencies": {
    "http-server": "^14.1.1",
    "prettier": "^3.1.0"
  }
}
